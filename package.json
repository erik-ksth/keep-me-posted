{
  "name": "context-window-inspector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local, Codex-only context window composition inspector.",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check src/analyzer.js && node --check src/app.js && node --check src/session-catalog.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
