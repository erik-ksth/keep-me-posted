{
  "name": "context-window-inspector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local, Codex-only context window composition inspector.",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/analyzer.js && node --check src/app.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
